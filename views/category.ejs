<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= category %></title>
</head>
<body>
  <%- include('./partials/nav') %>
  <h1><%= category %></h1>
  <% menuItems.forEach(item => { %> <!--shows only the items that belong to the category chosen-->
    <h2><%= item.name %> - $<%= item.price %></h2>
    <p>Rating: <%= item.rating %>/5</p>
    <p><%= item.details %></p>
  <% }) %>
</body>
</html>
